# Use the local Python dev environment; the Nix devShell remains opt-in via `nix develop`.
if [[ ! -x .venv/bin/python ]]; then
  echo "Missing .venv; run: uv sync --locked --extra dev" >&2
  exit 1
fi
export VIRTUAL_ENV="$PWD/.venv"
PATH_add "$VIRTUAL_ENV/bin"
